name: Assistant Mesh Compatibility

on:
  push:
    branches: [main, continuation/30-assistant-mesh-main-flutter]
  pull_request:
  workflow_dispatch:

env:
  ENTERPRISE_MESH_REF: continuation/30-assistant-mesh-main-flutter

jobs:
  validate-mesh:
    name: Validate Enterprise ↔ Flutter 30-unit mesh
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Flutter workspace
        uses: actions/checkout@v4

      - name: Fetch Enterprise mesh manifest on coordinated ref
        shell: bash
        run: |
          set -euo pipefail
          url="https://raw.githubusercontent.com/phakphoum38-stack/ENTERPRISE_API_ARCHITECTURE_LOGIC_TH/${ENTERPRISE_MESH_REF}/docs/assistant-mesh-30.yml"
          curl --fail --silent --show-error --location "$url" -o /tmp/enterprise-assistant-mesh-30.yml

      - name: Fetch pinned Enterprise contract
        shell: bash
        run: |
          set -euo pipefail
          url="https://raw.githubusercontent.com/phakphoum38-stack/ENTERPRISE_API_ARCHITECTURE_LOGIC_TH/${ENTERPRISE_MESH_REF}/current/CONTRACTS/contract-manifest.yml"
          curl --fail --silent --show-error --location "$url" -o /tmp/contract-manifest.yml
          grep -q 'anef-core-contract' /tmp/contract-manifest.yml
          grep -q 'version: 0.1.0' /tmp/contract-manifest.yml

      - name: Validate mesh contract and unit counts
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import re

          main = Path('/tmp/enterprise-assistant-mesh-30.yml').read_text()
          flutter = Path('docs/assistant-mesh-30.yml').read_text()

          def ids(text, prefix):
              return re.findall(r'^\s+- id: (' + re.escape(prefix) + r'-\d{2})\s*$', text, re.M)

          main_ids = ids(main, 'MAIN')
          flutter_ids = ids(flutter, 'FLUTTER')

          assert 'contract: anef-core-contract' in main
          assert 'contract_version: 0.1.0' in main
          assert 'upstream_contract: anef-core-contract' in flutter
          assert 'upstream_contract_version: 0.1.0' in flutter
          assert 'logical_units_only: true' in flutter
          assert 'bounded_execution: true' in flutter
          assert 'fail_closed_on_upstream_mismatch: true' in flutter
          assert len(main_ids) == 10, main_ids
          assert len(flutter_ids) == 20, flutter_ids
          assert len(set(main_ids)) == 10
          assert len(set(flutter_ids)) == 20

          for i in range(1, 11):
              assert f'MAIN-{i:02d}' in main_ids
          for i in range(1, 21):
              assert f'FLUTTER-{i:02d}' in flutter_ids

          print('Enterprise units:', len(main_ids))
          print('Flutter units:', len(flutter_ids))
          print('Total logical units:', len(main_ids) + len(flutter_ids))
          print('Contract: anef-core-contract:0.1.0')
          print('Execution model: logical units + bounded execution')
          PY
